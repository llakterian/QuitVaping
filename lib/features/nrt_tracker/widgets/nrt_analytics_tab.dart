import 'package:flutter/material.dart';

class NRTAnalyticsTab extends StatelessWidget {
  const NRTAnalyticsTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('NRT Analytics coming soon'),
    );
  }
}
